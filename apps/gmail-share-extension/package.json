{
	"name": "@cap/gmail-share-extension",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && vite build",
		"dev": "rm -rf dist && vite build --watch --mode development",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.323",
		"typescript": "^5.8.3",
		"vite": "6.3.5"
	}
}
